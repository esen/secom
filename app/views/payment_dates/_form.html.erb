<%= form_for(@payment_date) do |f| %>
  <% if @payment_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_date.errors.count, "error") %> prohibited this payment_date from being saved:</h2>

      <ul>
      <% @payment_date.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br />
    <%= f.number_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :payment_date %><br />
    <%= f.date_select :payment_date %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
