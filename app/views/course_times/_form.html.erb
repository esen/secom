<%= form_for(@course_time) do |f| %>
  <% if @course_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_time.errors.count, "error") %> prohibited this course_time from being saved:</h2>

      <ul>
      <% @course_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :starts_at %><br />
    <%= f.text_field :starts_at %>
  </div>
  <div class="field">
    <%= f.label :ends_at %><br />
    <%= f.text_field :ends_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
