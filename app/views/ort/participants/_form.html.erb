<%= form_for(@ort_participant) do |f| %>
  <% if @ort_participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ort_participant.errors.count, "error") %> prohibited this ort_participant from being saved:</h2>

      <ul>
      <% @ort_participant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :registered_at %><br />
    <%= f.datetime_select :registered_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
