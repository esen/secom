<%- model_class = Ort::Exam -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:cost) %></th>
      <th><%= model_class.human_attribute_name(:exam_type1_id) %></th>
      <th><%= model_class.human_attribute_name(:exam_type2_id) %></th>
      <th><%= model_class.human_attribute_name(:exam_type3_id) %></th>
      <th><%= model_class.human_attribute_name(:exam_type4_id) %></th>
      <th><%= model_class.human_attribute_name(:exam_type5_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @exams.each do |exam| %>
      <tr>
        <td><%= link_to exam.id, ort_exam_path(exam) %></td>
        <td><%= exam.cost %></td>
        <td><%= exam.exam_type1_id %></td>
        <td><%= exam.exam_type2_id %></td>
        <td><%= exam.exam_type3_id %></td>
        <td><%= exam.exam_type4_id %></td>
        <td><%= exam.exam_type5_id %></td>
        <td><%=l exam.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_ort_exam_path(exam), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      ort_exam_path(exam),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_ort_exam_path,
            :class => 'btn btn-primary' %>
